export interface ICustomCastData {
    /**
     * Total duration of the media in seconds
     */
    durationSeconds?: number;

    /**
     * Seconds into the file that the stream started
     */
    startTimeAbsolute: number;
}
